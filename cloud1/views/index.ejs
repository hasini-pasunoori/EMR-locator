<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Emergency Medical Resource Locator - Save Lives Instantly</title>
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&family=Open+Sans:wght@300;400;500;600;700;800&family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    
    <!-- GSAP -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.11.4/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.11.4/ScrollTrigger.min.js"></script>
    
    <style>
       
        :root {
            --primary-red: #e63946;
            --primary-blue: #457b9d;
            --bg-white: #f1faee;
            --dark-bg: #1d3557;
            --light-gray: #f8f9fa;
            --accent-orange: #f77f00;
            --accent-green: #2a9d8f;
            --glass-bg: rgba(255, 255, 255, 0.1);
            --glass-border: rgba(255, 255, 255, 0.2);
            --shadow-light: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
            --gradient-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --gradient-emergency: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
            --gradient-medical: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%);
            --gradient-success: linear-gradient(135deg, #00b894 0%, #00a085 100%);
            --neon-glow: 0 0 20px rgba(230, 57, 70, 0.6);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            overflow-x: hidden;
            background: #0a0a0a;
            color: white;
            cursor: none;
        }

        /* Custom Cursor */
        .cursor {
            width: 20px;
            height: 20px;
            border: 2px solid var(--primary-red);
            border-radius: 50%;
            position: fixed;
            top: 0;
            left: 0;
            pointer-events: none;
            z-index: 9999;
            transition: all 0.1s ease;
        }

        .cursor-follower {
            width: 8px;
            height: 8px;
            background: var(--primary-red);
            border-radius: 50%;
            position: fixed;
            top: 0;
            left: 0;
            pointer-events: none;
            z-index: 9998;
            transition: all 0.15s ease;
        }

        /* Smooth Scroll */
        html {
            scroll-behavior: smooth;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Poppins', sans-serif;
        }

        /* Loading Screen */
        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 10000;
            flex-direction: column;
        }

        .loading-logo {
            font-size: 4rem;
            color: var(--primary-red);
            margin-bottom: 2rem;
            animation: pulse-glow 2s infinite;
        }

        .loading-text {
            font-size: 1.5rem;
            color: white;
            font-weight: 300;
            opacity: 0.7;
        }

        .loading-progress {
            width: 300px;
            height: 4px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 2px;
            margin-top: 2rem;
            overflow: hidden;
        }

        .loading-bar {
            height: 100%;
            background: var(--gradient-emergency);
            width: 0%;
            transition: width 0.3s ease;
        }

        @keyframes pulse-glow {
            0%, 100% { 
                text-shadow: 0 0 20px var(--primary-red), 0 0 40px var(--primary-red);
                transform: scale(1);
            }
            50% { 
                text-shadow: 0 0 30px var(--primary-red), 0 0 60px var(--primary-red);
                transform: scale(1.05);
            }
        }

        /* Navigation */
        .navbar {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(10, 10, 10, 0.95);
            backdrop-filter: blur(20px);
            z-index: 1000;
            transition: all 0.3s ease;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .navbar-brand {
            font-family: 'Poppins', sans-serif;
            font-weight: 700;
            font-size: 1.5rem;
            color: var(--primary-red) !important;
            text-shadow: 0 0 10px rgba(230, 57, 70, 0.5);
        }

        .nav-link {
            color: rgba(255, 255, 255, 0.8) !important;
            font-weight: 500;
            margin: 0 1rem;
            position: relative;
            transition: all 0.3s ease;
        }

        .nav-link:hover {
            color: var(--primary-red) !important;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -5px;
            left: 50%;
            background: var(--primary-red);
            transition: all 0.3s ease;
        }

        .nav-link:hover::after {
            width: 100%;
            left: 0;
        }

        .auth-buttons {
            display: flex;
            gap: 1rem;
        }

        .btn-auth {
            padding: 0.5rem 1.5rem;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .btn-signin {
            background: transparent;
            border: 2px solid var(--primary-blue);
            color: var(--primary-blue);
        }

        .btn-signin:hover {
            background: var(--primary-blue);
            color: white;
        }

        .btn-signup {
            background: var(--gradient-emergency);
            border: none;
            color: white;
        }

        .btn-signup:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(230, 57, 70, 0.3);
        }

        /* Hero Section */
        .hero-section {
            height: 100vh;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
        }

        .hero-particles {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        .particle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: var(--primary-red);
            border-radius: 50%;
            animation: float 6s infinite linear;
            opacity: 0.6;
        }

        @keyframes float {
            0% {
                transform: translateY(100vh) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 0.6;
            }
            90% {
                opacity: 0.6;
            }
            100% {
                transform: translateY(-100px) rotate(360deg);
                opacity: 0;
            }
        }

        .floating-icon {
            position: absolute;
            color: var(--primary-red);
            opacity: 0.1;
            font-size: 3rem;
            animation: float-icon 8s infinite ease-in-out;
        }

        @keyframes float-icon {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            25% { transform: translateY(-20px) rotate(90deg); }
            50% { transform: translateY(0px) rotate(180deg); }
            75% { transform: translateY(-15px) rotate(270deg); }
        }

        .hero-content {
            position: relative;
            z-index: 2;
        }

        .hero-title {
            font-size: 5rem;
            font-weight: 900;
            background: linear-gradient(135deg, #fff 0%, var(--primary-red) 50%, #fff 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 1rem;
            opacity: 0;
            transform: translateY(100px);
            text-shadow: 0 0 30px rgba(255, 255, 255, 0.3);
        }

        .hero-subtitle {
            font-size: 1.8rem;
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 3rem;
            opacity: 0;
            transform: translateY(50px);
            font-weight: 300;
            line-height: 1.6;
        }

        .hero-stats {
            display: flex;
            justify-content: center;
            gap: 3rem;
            margin-bottom: 3rem;
            opacity: 0;
            transform: translateY(30px);
        }

        .stat-item {
            text-align: center;
            background: rgba(255, 255, 255, 0.05);
            padding: 1.5rem;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--primary-red);
            display: block;
        }

        .stat-label {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.6);
            margin-top: 0.5rem;
        }

        .btn-hero {
            position: relative;
            padding: 18px 40px;
            font-size: 1.2rem;
            font-weight: 600;
            border-radius: 50px;
            border: none;
            cursor: pointer;
            transition: all 0.3s ease;
            overflow: hidden;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin: 0 1rem;
        }

        .btn-primary-red {
            background: var(--gradient-emergency);
            color: white;
            box-shadow: 0 10px 25px rgba(230, 57, 70, 0.3);
        }

        .btn-primary-red:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(230, 57, 70, 0.5);
        }

        .btn-outline-blue {
            background: transparent;
            color: var(--primary-blue);
            border: 2px solid var(--primary-blue);
            box-shadow: 0 10px 25px rgba(69, 123, 157, 0.2);
        }

        .btn-outline-blue:hover {
            background: var(--primary-blue);
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 0 30px rgba(69, 123, 157, 0.6);
        }

        .pulse-effect {
            animation: pulse-button 2s infinite;
        }

        @keyframes pulse-button {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        /* Glassmorphism Cards */
        .glass-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 2rem;
            transition: all 0.3s ease;
        }

        .glass-card:hover {
            background: rgba(255, 255, 255, 0.08);
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        }

        /* Section Styling */
        section {
            padding: 120px 0;
            position: relative;
        }

        .section-title {
            font-size: 4rem;
            font-weight: 700;
            text-align: center;
            margin-bottom: 4rem;
            background: linear-gradient(135deg, #fff 0%, var(--primary-red) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .section-subtitle {
            font-size: 1.3rem;
            color: rgba(255, 255, 255, 0.7);
            text-align: center;
            margin-bottom: 4rem;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
            line-height: 1.8;
        }

        /* Problem Solution Section */
        .problem-solution {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            position: relative;
        }

        .morphing-shape {
            position: absolute;
            width: 200px;
            height: 200px;
            background: var(--gradient-emergency);
            border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
            opacity: 0.1;
            animation: morph 8s ease-in-out infinite;
        }

        @keyframes morph {
            0%, 100% {
                border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
                transform: rotate(0deg);
            }
            25% {
                border-radius: 58% 42% 75% 25% / 76% 46% 54% 24%;
                transform: rotate(90deg);
            }
            50% {
                border-radius: 50% 50% 33% 67% / 55% 27% 73% 45%;
                transform: rotate(180deg);
            }
            75% {
                border-radius: 33% 67% 58% 42% / 63% 68% 32% 37%;
                transform: rotate(270deg);
            }
        }

        .problem-illustration {
            font-size: 10rem;
            text-align: center;
            margin-bottom: 2rem;
            position: relative;
        }

        .problem-icon, .solution-icon {
            transition: all 0.5s ease;
            filter: drop-shadow(0 0 20px currentColor);
        }

        .arrow-transform {
            font-size: 3rem;
            color: var(--primary-red);
            margin: 2rem 0;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-10px); }
            60% { transform: translateY(-5px); }
        }

        /* Features Section */
        .features-section {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 100%);
            position: relative;
        }

        .feature-card {
            background: rgba(255, 255, 255, 0.02);
            backdrop-filter: blur(20px);
            border-radius: 25px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 3rem 2rem;
            text-align: center;
            transition: all 0.5s ease;
            height: 100%;
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transition: left 0.5s ease;
        }

        .feature-card:hover::before {
            left: 100%;
        }

        .feature-card:hover {
            transform: translateY(-15px) scale(1.02);
            background: rgba(255, 255, 255, 0.05);
            box-shadow: 0 25px 50px rgba(230, 57, 70, 0.2);
            border-color: var(--primary-red);
        }

        .feature-icon {
            font-size: 4rem;
            margin-bottom: 1.5rem;
            background: var(--gradient-emergency);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: drop-shadow(0 0 10px rgba(230, 57, 70, 0.5));
        }

        .feature-title {
            font-size: 1.8rem;
            font-weight: 600;
            color: white;
            margin-bottom: 1rem;
        }

        .feature-description {
            color: rgba(255, 255, 255, 0.7);
            line-height: 1.6;
            font-size: 1.1rem;
        }

        /* Interactive 3D Cards */
        .card-3d {
            perspective: 1000px;
        }

        .card-inner {
            position: relative;
            width: 100%;
            height: 100%;
            text-align: center;
            transition: transform 0.8s;
            transform-style: preserve-3d;
        }

        .card-3d:hover .card-inner {
            transform: rotateY(180deg);
        }

        .card-front, .card-back {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            border-radius: 25px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }

        .card-back {
            background: var(--gradient-emergency);
            transform: rotateY(180deg);
            color: white;
        }

        /* Advanced Timeline */
        .timeline-section {
            background: linear-gradient(135deg, #16213e 0%, #0a0a0a 100%);
            position: relative;
        }

        .timeline-container {
            position: relative;
            max-width: 1200px;
            margin: 0 auto;
        }

        .timeline-line {
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
            width: 4px;
            height: 100%;
            background: linear-gradient(to bottom, var(--primary-red), var(--primary-blue));
            border-radius: 2px;
        }

        .timeline-item {
            display: flex;
            justify-content: flex-end;
            padding-right: 30px;
            position: relative;
            margin: 50px 0;
            width: 50%;
            opacity: 0;
            transform: translateX(-100px);
        }

        .timeline-item:nth-child(even) {
            align-self: flex-end;
            justify-content: flex-start;
            padding-left: 30px;
            padding-right: 0;
            transform: translateX(100px);
        }

        .timeline-content {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            padding: 2rem;
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            max-width: 400px;
            position: relative;
            transition: all 0.3s ease;
        }

        .timeline-content:hover {
            background: rgba(255, 255, 255, 0.08);
            transform: scale(1.05);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        }

        .timeline-icon {
            position: absolute;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 60px;
            height: 60px;
            background: var(--gradient-emergency);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            color: white;
            z-index: 2;
            top: 50%;
            box-shadow: 0 0 20px rgba(230, 57, 70, 0.5);
        }

        .timeline-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: white;
            margin-bottom: 1rem;
        }

        .timeline-description {
            color: rgba(255, 255, 255, 0.7);
            line-height: 1.6;
        }

        /* Interactive Map Section */
        .map-section {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            position: relative;
        }

        .interactive-map {
            width: 100%;
            height: 500px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 20px;
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .map-pulse {
            position: absolute;
            width: 20px;
            height: 20px;
            background: var(--primary-red);
            border-radius: 50%;
            animation: map-pulse 2s infinite;
        }

        @keyframes map-pulse {
            0% {
                box-shadow: 0 0 0 0 rgba(230, 57, 70, 0.7);
            }
            70% {
                box-shadow: 0 0 0 20px rgba(230, 57, 70, 0);
            }
            100% {
                box-shadow: 0 0 0 0 rgba(230, 57, 70, 0);
            }
        }

        /* Impact Section with Advanced Counters */
        .impact-section {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 100%);
            position: relative;
        }

        .impact-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><circle cx="50" cy="50" r="2" fill="%23e63946" opacity="0.1"/></svg>') repeat;
            animation: bg-move 20s linear infinite;
        }

        @keyframes bg-move {
            0% { transform: translate(0, 0); }
            100% { transform: translate(-100px, -100px); }
        }

        .counter-card {
            background: rgba(255, 255, 255, 0.02);
            backdrop-filter: blur(20px);
            border-radius: 25px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 3rem 2rem;
            text-align: center;
            transition: all 0.5s ease;
            position: relative;
            overflow: hidden;
        }

        .counter-card:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.05);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
        }

        .counter {
            font-size: 4rem;
            font-weight: 800;
            background: var(--gradient-emergency);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            display: block;
            margin-bottom: 1rem;
        }

        .counter-label {
            font-size: 1.2rem;
            color: rgba(255, 255, 255, 0.8);
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .counter-description {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.5);
            margin-top: 1rem;
            line-height: 1.6;
        }

        /* Testimonials Section */
        .testimonials-section {
            background: linear-gradient(135deg, #16213e 0%, #0a0a0a 100%);
            position: relative;
        }

        .testimonial-carousel {
            position: relative;
            overflow: hidden;
            border-radius: 20px;
            max-width: 800px;
            margin: 0 auto;
        }

        .testimonial-slide {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 3rem;
            text-align: center;
            border-radius: 20px;
            margin: 1rem;
        }

        .testimonial-text {
            font-size: 1.3rem;
            color: rgba(255, 255, 255, 0.8);
            line-height: 1.8;
            margin-bottom: 2rem;
            font-style: italic;
        }

        .testimonial-author {
            font-size: 1.1rem;
            color: var(--primary-red);
            font-weight: 600;
        }

        .testimonial-role {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.5);
            margin-top: 0.5rem;
        }

        /* CTA Section */
        .cta-section {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            position: relative;
            text-align: center;
            padding: 100px 0;
        }

        .cta-glow {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(230, 57, 70, 0.1) 0%, transparent 70%);
            border-radius: 50%;
        }

        .cta-content {
            position: relative;
            z-index: 2;
            max-width: 800px;
            margin: 0 auto;
        }

        .cta-title {
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, #fff 0%, var(--primary-red) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .cta-subtitle {
            font-size: 1.3rem;
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 3rem;
            line-height: 1.6;
        }

        .cta-buttons {
            display: flex;
            justify-content: center;
            gap: 2rem;
            flex-wrap: wrap;
        }

        /* Footer */
        footer {
            background: #000;
            color: white;
            padding: 80px 0 40px;
            position: relative;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 3rem;
            margin-bottom: 3rem;
        }

        .footer-section h4 {
            color: var(--primary-red);
            font-size: 1.3rem;
            margin-bottom: 1.5rem;
            font-weight: 600;
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 0.8rem;
        }

        .footer-links a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .footer-links a:hover {
            color: var(--primary-red);
            transform: translateX(5px);
        }

        .social-links {
            display: flex;
            gap: 1rem;
        }

        .social-link {
            width: 50px;
            height: 50px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            text-decoration: none;
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .social-link:hover {
            background: var(--primary-red);
            color: white;
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(230, 57, 70, 0.3);
        }

        .footer-bottom {
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            padding-top: 2rem;
            text-align: center;
            color: rgba(255, 255, 255, 0.5);
        }

        /* Authentication Modal */
        .modal-content {
            background: rgba(26, 26, 46, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            color: white;
        }

        .modal-header {
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .modal-title {
            color: var(--primary-red);
            font-weight: 700;
        }

        .btn-close {
            filter: invert(1);
        }

        .form-control {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: white;
            border-radius: 10px;
            padding: 12px 15px;
        }

        .form-control:focus {
            background: rgba(255, 255, 255, 0.08);
            border-color: var(--primary-red);
            box-shadow: 0 0 0 0.25rem rgba(230, 57, 70, 0.25);
            color: white;
        }

        .form-label {
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 0.5rem;
        }

        .form-check-input {
            background-color: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .form-check-input:checked {
            background-color: var(--primary-red);
            border-color: var(--primary-red);
        }

        .form-check-label {
            color: rgba(255, 255, 255, 0.7);
        }

        .auth-switch {
            text-align: center;
            margin-top: 1.5rem;
            color: rgba(255, 255, 255, 0.7);
        }

        .auth-switch a {
            color: var(--primary-red);
            text-decoration: none;
            font-weight: 600;
        }

        .auth-switch a:hover {
            text-decoration: underline;
        }

        /* Mobile Responsiveness */
        @media (max-width: 768px) {
            .hero-title {
                font-size: 3rem;
            }
            
            .hero-subtitle {
                font-size: 1.3rem;
            }
            
            .section-title {
                font-size: 2.5rem;
            }
            
            .hero-stats {
                flex-direction: column;
                gap: 1rem;
            }
            
            .timeline-item {
                width: 100%;
                padding-left: 0;
                padding-right: 0;
                justify-content: center;
            }
            
            .timeline-item:nth-child(even) {
                justify-content: center;
            }
            
            .btn-hero {
                margin: 0.5rem 0;
            }
            
            .problem-illustration {
                font-size: 6rem;
            }
            
            .feature-card {
                margin-bottom: 2rem;
            }

            .auth-buttons {
                display: none;
            }

            .mobile-auth {
                display: block !important;
            }

            .cta-title {
                font-size: 2.5rem;
            }

            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }
        }

        .mobile-auth {
            display: none;
        }

        /* New styles for additional sections */
        .mvp-features-section {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 100%);
            position: relative;
        }

        .mvp-feature-card {
            background: rgba(255, 255, 255, 0.02);
            backdrop-filter: blur(20px);
            border-radius: 25px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 2rem;
            text-align: center;
            transition: all 0.5s ease;
            height: 100%;
            position: relative;
            overflow: hidden;
        }

        .mvp-feature-card:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.05);
            box-shadow: 0 15px 30px rgba(230, 57, 70, 0.2);
            border-color: var(--primary-red);
        }

        .mvp-feature-icon {
            font-size: 3rem;
            margin-bottom: 1.5rem;
            color: var(--primary-red);
        }

        .mvp-feature-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: white;
            margin-bottom: 1rem;
        }

        .mvp-feature-description {
            color: rgba(255, 255, 255, 0.7);
            line-height: 1.6;
        }

        .future-expansion-section {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            position: relative;
        }

        .expansion-item {
            background: rgba(255, 255, 255, 0.02);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 2rem;
            margin-bottom: 2rem;
            transition: all 0.3s ease;
            opacity: 0;
            transform: translateY(50px);
        }

        .expansion-item:hover {
            background: rgba(255, 255, 255, 0.05);
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        }

        .expansion-icon {
            font-size: 2.5rem;
            color: var(--primary-red);
            margin-bottom: 1rem;
        }

        .expansion-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: white;
            margin-bottom: 1rem;
        }

        .expansion-description {
            color: rgba(255, 255, 255, 0.7);
            line-height: 1.6;
        }

        .why-matters-section {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 100%);
            position: relative;
        }

        .impact-statement {
            font-size: 1.8rem;
            color: rgba(255, 255, 255, 0.9);
            text-align: center;
            max-width: 900px;
            margin: 0 auto 4rem;
            line-height: 1.8;
            font-weight: 300;
        }

        .impact-highlight {
            color: var(--primary-red);
            font-weight: 600;
        }

        .tech-stack {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 2rem;
            margin-top: 4rem;
        }

        .tech-item {
            background: rgba(255, 255, 255, 0.02);
            backdrop-filter: blur(20px);
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 1.5rem;
            text-align: center;
            min-width: 120px;
            transition: all 0.3s ease;
        }

        .tech-item:hover {
            background: rgba(255, 255, 255, 0.05);
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        }

        .tech-icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: var(--primary-red);
        }

        .tech-name {
            font-size: 1rem;
            color: white;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <!-- Custom Cursor -->
    <div class="cursor"></div>
    <div class="cursor-follower"></div>

    <!-- Loading Screen -->
    <div class="loading-screen">
        <div class="loading-logo">
            <i class="fas fa-heartbeat"></i>
        </div>
        <div class="loading-text">Loading Emergency Medical Resource Locator</div>
        <div class="loading-progress">
            <div class="loading-bar"></div>
        </div>
    </div>

    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="#">
                <i class="fas fa-heartbeat me-2"></i>EMResource
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="#home">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#problem">Problem</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#features">Features</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#future">Future</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#impact">Impact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#contact">Contact</a>
                    </li>
                </ul>
                
                <div class="auth-buttons ms-3">
                    <button class="btn btn-auth btn-signin" data-bs-toggle="modal" data-bs-target="#loginModal">Sign In</button>
                    <button class="btn btn-auth btn-signup" data-bs-toggle="modal" data-bs-target="#registerModal">Sign Up</button>
                </div>
                
                <div class="mobile-auth">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="#" data-bs-toggle="modal" data-bs-target="#loginModal">Sign In</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#" data-bs-toggle="modal" data-bs-target="#registerModal">Sign Up</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero-section" id="home">
        <div class="hero-particles" id="particles"></div>
        <div class="floating-icon" style="top: 20%; left: 10%;">
            <i class="fas fa-heartbeat"></i>
        </div>
        <div class="floating-icon" style="top: 60%; left: 85%;">
            <i class="fas fa-hospital"></i>
        </div>
        <div class="floating-icon" style="top: 30%; left: 75%;">
            <i class="fas fa-first-aid"></i>
        </div>
        <div class="floating-icon" style="top: 70%; left: 15%;">
            <i class="fas fa-ambulance"></i>
        </div>
        <div class="container hero-content">
            <div class="row justify-content-center text-center">
                <div class="col-lg-10">
                    <h1 class="hero-title">Find Medical Help Instantly</h1>
                    <p class="hero-subtitle">Locate hospitals, pharmacies, blood donors, and oxygen suppliers near you — anytime, anywhere.</p>
                    
                    <div class="hero-stats">
                        <div class="stat-item">
                            <span class="stat-number" id="counter-hospitals">0</span>
                            <span class="stat-label">Hospitals</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-number" id="counter-donors">0</span>
                            <span class="stat-label">Blood Donors</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-number" id="counter-users">0</span>
                            <span class="stat-label">Active Users</span>
                        </div>
                    </div>
                    
                    <div class="hero-buttons">
                        <button class="btn btn-hero btn-primary-red pulse-effect">
                            <i class="fas fa-map-marker-alt me-2"></i> Find Resources
                        </button>
                        <button class="btn btn-hero btn-outline-blue">
                            <i class="fas fa-hand-holding-heart me-2"></i> Join as Donor
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Problem & Solution Section -->
    <section class="problem-solution section-padding" id="problem">
        <div class="morphing-shape" style="top: 10%; left: 5%;"></div>
        <div class="morphing-shape" style="bottom: 10%; right: 5%;"></div>
        
        <div class="container">
            <h2 class="section-title">Emergency Response Solution</h2>
            <p class="section-subtitle">We're addressing the critical challenge of finding medical resources during emergencies when every second counts.</p>
            
            <div class="row align-items-center">
                <div class="col-lg-6 text-center">
                    <div class="problem-illustration">
                        <i class="fas fa-user-injured problem-icon" style="color: #e63946;"></i>
                        <div class="arrow-transform">
                            <i class="fas fa-long-arrow-alt-right"></i>
                        </div>
                        <i class="fas fa-map-marked-alt solution-icon" style="color: #457b9d;"></i>
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="glass-card">
                        <h3 class="mb-4">The Critical Problem</h3>
                        <p class="mb-4">During medical emergencies, people often struggle to find nearby healthcare facilities, blood donors, or medical supplies. Precious time is wasted searching for resources when every second counts.</p>
                        
                        <h3 class="mb-4">Our Solution</h3>
                        <p>EMResource provides instant access to a comprehensive database of medical resources. With real-time location data and availability information, we connect those in need with life-saving resources faster than ever before.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- MVP Features Section -->
    <section class="mvp-features-section section-padding" id="features">
        <div class="container">
            <h2 class="section-title">Key Features</h2>
            <p class="section-subtitle">Our platform offers essential tools to help you find medical resources quickly during emergencies.</p>
            
            <div class="row">
                <div class="col-md-6 col-lg-3 mb-4">
                    <div class="mvp-feature-card">
                        <div class="mvp-feature-icon">
                            <i class="fas fa-hospital"></i>
                        </div>
                        <h3 class="mvp-feature-title">Nearby Hospitals</h3>
                        <p class="mvp-feature-description">Find the closest hospitals with emergency services, contact information, and real-time wait times.</p>
                    </div>
                </div>
                
                <div class="col-md-6 col-lg-3 mb-4">
                    <div class="mvp-feature-card">
                        <div class="mvp-feature-icon">
                            <i class="fas fa-pills"></i>
                        </div>
                        <h3 class="mvp-feature-title">Pharmacies</h3>
                        <p class="mvp-feature-description">Locate nearby pharmacies with information on available medications and operating hours.</p>
                    </div>
                </div>
                
                <div class="col-md-6 col-lg-3 mb-4">
                    <div class="mvp-feature-card">
                        <div class="mvp-feature-icon">
                            <i class="fas fa-tint"></i>
                        </div>
                        <h3 class="mvp-feature-title">Blood Donors</h3>
                        <p class="mvp-feature-description">Connect with verified blood donors by blood type and location in critical situations.</p>
                    </div>
                </div>
                
                <div class="col-md-6 col-lg-3 mb-4">
                    <div class="mvp-feature-card">
                        <div class="mvp-feature-icon">
                            <i class="fas fa-map-marked-alt"></i>
                        </div>
                        <h3 class="mvp-feature-title">Interactive Maps</h3>
                        <p class="mvp-feature-description">Cloud-based directory with interactive maps for easy navigation to medical facilities.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>


    <section class="future-expansion-section section-padding" id="future">
        <div class="container">
            <h2 class="section-title">Future Expansion</h2>
            <p class="section-subtitle">We're continuously working to enhance our platform with new features to save more lives.</p>
            
            <div class="row">
                <div class="col-lg-6">
                    <div class="expansion-item">
                        <div class="expansion-icon">
                            <i class="fas fa-bell"></i>
                        </div>
                        <h3 class="expansion-title">Live Availability Updates</h3>
                        <p class="expansion-description">Real-time tracking of bed availability, medical equipment, and specialist doctors at healthcare facilities.</p>
                    </div>
                </div>
                
                <div class="col-lg-6">
                    <div class="expansion-item">
                        <div class="expansion-icon">
                            <i class="fas fa-exclamation-triangle"></i>
                        </div>
                        <h3 class="expansion-title">SOS Alerts</h3>
                        <p class="expansion-description">Instant emergency alerts to nearby hospitals and first responders with your location and medical information.</p>
                    </div>
                </div>
                
                <div class="col-lg-6">
                    <div class="expansion-item">
                        <div class="expansion-icon">
                            <i class="fas fa-comments"></i>
                        </div>
                        <h3 class="expansion-title">Chat Feature</h3>
                        <p class="expansion-description">Direct communication with healthcare providers for preliminary advice before reaching the facility.</p>
                    </div>
                </div>
                
                <div class="col-lg-6">
                    <div class="expansion-item">
                        <div class="expansion-icon">
                            <i class="fas fa-robot"></i>
                        </div>
                        <h3 class="expansion-title">AI Triage Assistant</h3>
                        <p class="expansion-description">Artificial intelligence-powered symptom assessment to recommend the most appropriate care level.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="why-matters-section section-padding" id="impact">
        <div class="container">
            <h2 class="section-title">Why It Matters</h2>
            
            <p class="impact-statement">This project is <span class="impact-highlight">socially impactful</span>, addressing a critical need in healthcare accessibility while utilizing <span class="impact-highlight">cutting-edge cloud technologies</span> and database management to save lives.</p>
            
            <div class="row">
                <div class="col-md-4 mb-4">
                    <div class="counter-card">
                        <span class="counter" data-target="500">0</span>
                        <span class="counter-label">Hospitals Registered</span>
                        <p class="counter-description">Partner healthcare facilities across multiple regions</p>
                    </div>
                </div>
                
                <div class="col-md-4 mb-4">
                    <div class="counter-card">
                        <span class="counter" data-target="1200">0</span>
                        <span class="counter-label">Blood Donors</span>
                        <p class="counter-description">Verified donors ready to help in emergencies</p>
                    </div>
                </div>
                
                <div class="col-md-4 mb-4">
                    <div class="counter-card">
                        <span class="counter" data-target="10000">0</span>
                        <span class="counter-label">Lives Impacted</span>
                        <p class="counter-description">People helped through our platform</p>
                    </div>
                </div>
            </div>
            
            <div class="tech-stack">
                <div class="tech-item">
                    <div class="tech-icon">
                        <i class="fas fa-database"></i>
                    </div>
                    <div class="tech-name">MongoDB</div>
                </div>
                
                <div class="tech-item">
                    <div class="tech-icon">
                        <i class="fas fa-cloud"></i>
                    </div>
                    <div class="tech-name">AWS</div>
                </div>
                
                <div class="tech-item">
                    <div class="tech-icon">
                        <i class="fas fa-server"></i>
                    </div>
                    <div class="tech-name">Node.js</div>
                </div>
                
                <div class="tech-item">
                    <div class="tech-icon">
                        <i class="fas fa-map-marked-alt"></i>
                    </div>
                    <div class="tech-name">Google Maps API</div>
                </div>
                
                <div class="tech-item">
                    <div class="tech-icon">
                        <i class="fas fa-mobile-alt"></i>
                    </div>
                    <div class="tech-name">React Native</div>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="cta-glow"></div>
        <div class="container cta-content">
            <h2 class="cta-title">Join Our Mission to Save Lives</h2>
            <p class="cta-subtitle">Create an account today to access emergency medical resources or contribute as a donor to help those in need.</p>
            
            <div class="cta-buttons">
                <button class="btn btn-hero btn-primary-red" data-bs-toggle="modal" data-bs-target="#registerModal">
                    <i class="fas fa-user-plus me-2"></i> Sign Up Now
                </button>
                <button class="btn btn-hero btn-outline-blue">
                    <i class="fas fa-info-circle me-2"></i> Learn More
                </button>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer id="contact">
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h4>EMResource</h4>
                    <p>Connecting people with life-saving medical resources during emergencies.</p>
                </div>
                
                <div class="footer-section">
                    <h4>Quick Links</h4>
                    <ul class="footer-links">
                        <li><a href="#home">Home</a></li>
                        <li><a href="#problem">Problem & Solution</a></li>
                        <li><a href="#features">Features</a></li>
                        <li><a href="#future">Future Plans</a></li>
                        <li><a href="#impact">Impact</a></li>
                    </ul>
                </div>
                
                <div class="footer-section">
                    <h4>Contact Us</h4>
                    <ul class="footer-links">
                        <li><a href="mailto:info@emresource.com">info@emresource.com</a></li>
                        <li><a href="tel:+1234567890">+1 (234) 567-890</a></li>
                        <li>123 Medical Way, Healthcare City</li>
                    </ul>
                </div>
                
                <div class="footer-section">
                    <h4>Follow Us</h4>
                    <div class="social-links">
                        <a href="#" class="social-link"><i class="fab fa-github"></i></a>
                        <a href="#" class="social-link"><i class="fab fa-linkedin"></i></a>
                        <a href="#" class="social-link"><i class="fab fa-twitter"></i></a>
                        <a href="#" class="social-link"><i class="fab fa-facebook"></i></a>
                    </div>
                </div>
            </div>
            
            <div class="footer-bottom">
                <p>&copy; 2023 EMResource - Emergency Medical Resource Locator. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Login Modal -->
    <div class="modal fade" id="loginModal" tabindex="-1" aria-labelledby="loginModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="loginModalLabel">Sign In to EMResource</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="loginForm">
                        <div class="mb-3">
                            <label for="loginEmail" class="form-label">Email address</label>
                            <input type="email" class="form-control" id="loginEmail" placeholder="name@example.com" required>
                        </div>
                        <div class="mb-3">
                            <label for="loginPassword" class="form-label">Password</label>
                            <input type="password" class="form-control" id="loginPassword" placeholder="Enter your password" required>
                        </div>
                        <div class="mb-3">
                            <label for="loginRole" class="form-label">Login as</label>
                            <select class="form-control" id="loginRole" required>
                                <option value="">Select your role</option>
                                <option value="user">General User</option>
                                <option value="donor">Blood Donor</option>
                                <option value="hospital">Hospital Staff</option>
                                <option value="admin">Administrator</option>
                            </select>
                        </div>
                        <div class="mb-3 form-check">
                            <input type="checkbox" class="form-check-input" id="rememberMe">
                            <label class="form-check-label" for="rememberMe">Remember me</label>
                        </div>
                        <button type="submit" class="btn btn-primary-red w-100">Send OTP</button>
                    </form>
                    <div class="auth-switch">
                        Don't have an account? <a href="#" data-bs-toggle="modal" data-bs-target="#registerModal" data-bs-dismiss="modal">Sign Up</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Register Modal -->
    <div class="modal fade" id="registerModal" tabindex="-1" aria-labelledby="registerModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="registerModalLabel">Create an Account</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="registerForm">
                        <div class="mb-3">
                            <label for="registerName" class="form-label">Full Name</label>
                            <input type="text" class="form-control" id="registerName" placeholder="John Doe" required>
                        </div>
                        <div class="mb-3">
                            <label for="registerEmail" class="form-label">Email address</label>
                            <input type="email" class="form-control" id="registerEmail" placeholder="name@example.com" required>
                        </div>
                        <div class="mb-3">
                            <label for="registerPassword" class="form-label">Password</label>
                            <input type="password" class="form-control" id="registerPassword" placeholder="Create a password" required>
                        </div>
                        <div class="mb-3">
                            <label for="confirmPassword" class="form-label">Confirm Password</label>
                            <input type="password" class="form-control" id="confirmPassword" placeholder="Confirm your password" required>
                        </div>
                        <div class="mb-3">
                            <label for="userRole" class="form-label">I am registering as</label>
                            <select class="form-control" id="userRole" required>
                                <option value="">Select your role</option>
                                <option value="user">General User (Patient/Family)</option>
                                <option value="donor">Blood Donor</option>
                                <option value="hospital">Hospital Staff</option>
                                <option value="admin">System Administrator</option>
                            </select>
                        </div>
                        <div class="mb-3 form-check">
                            <input type="checkbox" class="form-check-input" id="termsAgree" required>
                            <label class="form-check-label" for="termsAgree">I agree to the <a href="#">Terms of Service</a> and <a href="#">Privacy Policy</a></label>
                        </div>
                        <button type="submit" class="btn btn-primary-red w-100">Send OTP</button>
                    </form>
                    <div class="auth-switch">
                        Already have an account? <a href="#" data-bs-toggle="modal" data-bs-target="#loginModal" data-bs-dismiss="modal">Sign In</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Toast Notification -->
    <div class="toast-container position-fixed top-0 end-0 p-3" style="z-index: 9999;">
        <div id="authToast" class="toast" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-header bg-success text-white">
                <i class="fas fa-check-circle me-2"></i>
                <strong class="me-auto">EMResource</strong>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="toast"></button>
            </div>
            <div class="toast-body">
                <span id="toastMessage">Message here</span>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Auth JS -->
    <script src="/js/auth.js"></script>
    
    <script>
        // Initialize GSAP
        gsap.registerPlugin(ScrollTrigger);
        
        // Wait for page to load
        window.addEventListener('load', function() {
            // Simulate loading progress
            let progress = 0;
            const loadingInterval = setInterval(() => {
                progress += Math.random() * 10;
                document.querySelector('.loading-bar').style.width = `${progress}%`;
                
                if (progress >= 100) {
                    clearInterval(loadingInterval);
                    // Hide loading screen with animation
                    gsap.to('.loading-screen', {
                        opacity: 0,
                        duration: 0.5,
                        onComplete: () => {
                            document.querySelector('.loading-screen').style.display = 'none';
                            initAnimations();
                        }
                    });
                }
            }, 200);
        });
        
        // Custom cursor
        document.addEventListener('mousemove', function(e) {
            gsap.to('.cursor', {
                x: e.clientX,
                y: e.clientY,
                duration: 0.1
            });
            
            gsap.to('.cursor-follower', {
                x: e.clientX,
                y: e.clientY,
                duration: 0.3
            });
        });
        
        // Create floating particles
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const particleCount = 50;
            
            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.classList.add('particle');
                
                // Random position and size
                const size = Math.random() * 4 + 2;
                particle.style.width = `${size}px`;
                particle.style.height = `${size}px`;
                
                // Random position
                particle.style.left = `${Math.random() * 100}%`;
                particle.style.top = `${Math.random() * 100}%`;
                
                // Random animation delay
                particle.style.animationDelay = `${Math.random() * 6}s`;
                
                particlesContainer.appendChild(particle);
            }
        }
        
        // Animated counters
        function animateCounters() {
            const counters = document.querySelectorAll('.counter');
            
            counters.forEach(counter => {
                const target = +counter.getAttribute('data-target');
                const count = { value: 0 };
                
                gsap.to(count, {
                    value: target,
                    duration: 2,
                    onUpdate: () => {
                        counter.textContent = Math.floor(count.value);
                    },
                    scrollTrigger: {
                        trigger: counter,
                        start: 'top 80%',
                        toggleActions: 'play none none none'
                    }
                });
            });
        }
        
        // Initialize all animations
        function initAnimations() {
            createParticles();
            
            // Hero section animations
            gsap.to('.hero-title', {
                opacity: 1,
                y: 0,
                duration: 1,
                ease: 'power3.out'
            });
            
            gsap.to('.hero-subtitle', {
                opacity: 1,
                y: 0,
                duration: 1,
                delay: 0.3,
                ease: 'power3.out'
            });
            
            gsap.to('.hero-stats', {
                opacity: 1,
                y: 0,
                duration: 1,
                delay: 0.6,
                ease: 'power3.out'
            });
            
            // Animate feature cards on scroll
            gsap.utils.toArray('.feature-card, .mvp-feature-card, .expansion-item').forEach(item => {
                gsap.to(item, {
                    opacity: 1,
                    y: 0,
                    duration: 0.8,
                    scrollTrigger: {
                        trigger: item,
                        start: 'top 85%',
                        toggleActions: 'play none none none'
                    }
                });
            });
            
            // Animate timeline items
            gsap.utils.toArray('.timeline-item').forEach(item => {
                gsap.to(item, {
                    opacity: 1,
                    x: 0,
                    duration: 0.8,
                    scrollTrigger: {
                        trigger: item,
                        start: 'top 85%',
                        toggleActions: 'play none none none'
                    }
                });
            });
            
            // Animate counter cards
            gsap.utils.toArray('.counter-card').forEach(card => {
                gsap.to(card, {
                    opacity: 1,
                    y: 0,
                    duration: 0.8,
                    scrollTrigger: {
                        trigger: card,
                        start: 'top 85%',
                        toggleActions: 'play none none none'
                    }
                });
            });
            
            // Start counter animations
            animateCounters();
            
            // Animate problem/solution icons
            gsap.to('.problem-icon', {
                rotation: 360,
                duration: 2,
                scrollTrigger: {
                    trigger: '.problem-illustration',
                    start: 'top center',
                    scrub: true
                }
            });
            
            gsap.to('.solution-icon', {
                scale: 1.2,
                duration: 2,
                scrollTrigger: {
                    trigger: '.problem-illustration',
                    start: 'top center',
                    scrub: true
                }
            });
        }
    </script>
</body>
</html>